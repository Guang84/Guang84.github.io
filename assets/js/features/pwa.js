let promptEvent=null;
function toast(message,action){let n=document.querySelector('[data-app-toast]');if(!n){n=document.createElement('div');n.className='app-toast';n.dataset.appToast='';n.setAttribute('role','status');document.body.append(n)}n.replaceChildren();const text=document.createElement('span');text.textContent=message;n.append(text);if(action){const b=document.createElement('button');b.type='button';b.textContent=action.label;b.addEventListener('click',action.run);n.append(b)}n.hidden=false;if(!action)setTimeout(()=>n.hidden=true,2600)}
export async function initPWA(){const install=document.querySelector('[data-install]');if(install){addEventListener('beforeinstallprompt',e=>{e.preventDefault();promptEvent=e;install.hidden=false});install.addEventListener('click',async()=>{if(!promptEvent)return;promptEvent.prompt();await promptEvent.userChoice;promptEvent=null;install.hidden=true});addEventListener('appinstalled',()=>{install.hidden=true;promptEvent=null;toast('GLab installed.')})}if(!('serviceWorker'in navigator))return;try{const reg=await navigator.serviceWorker.register('/service-worker.js');let refreshing=false;navigator.serviceWorker.addEventListener('controllerchange',()=>{if(refreshing)return;refreshing=true;location.reload()});const offer=worker=>toast('A newer GLab version is ready.',{label:'Update',run:()=>worker.postMessage({type:'SKIP_WAITING'})});if(reg.waiting&&navigator.serviceWorker.controller)offer(reg.waiting);reg.addEventListener('updatefound',()=>{const w=reg.installing;if(!w)return;w.addEventListener('statechange',()=>{if(w.state==='installed'&&navigator.serviceWorker.controller)offer(w)})})}catch{}addEventListener('offline',()=>toast('You are offline. Cached pages remain available.'));addEventListener('online',()=>toast('Connection restored.'))}
