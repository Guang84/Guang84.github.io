export function initReading(){if(document.body.classList.contains("content-page-shell")){const bar=document.createElement("div");bar.className="reading-progress";bar.setAttribute("aria-hidden","true");document.body.prepend(bar);const u=()=>{const m=Math.max(1,document.documentElement.scrollHeight-innerHeight);bar.style.setProperty("--reading-progress",`${Math.min(100,Math.max(0,scrollY/m*100))}%`)};addEventListener("scroll",u,{passive:true});addEventListener("resize",u,{passive:true});u()}const b=document.createElement("button");b.className="back-to-top";b.type="button";b.textContent="↑";b.setAttribute("aria-label","Back to top");b.hidden=true;document.body.append(b);b.addEventListener("click",()=>scrollTo({top:0,behavior:matchMedia("(prefers-reduced-motion: reduce)").matches?"auto":"smooth"}));const s=()=>{b.hidden=scrollY<Math.max(innerHeight,700)};addEventListener("scroll",s,{passive:true});s()}
