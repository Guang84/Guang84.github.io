{
  "id": "ssh-fleet-doctor-setup",
  "title": "Set up SSH Fleet Doctor safely",
  "summary": "Prepare the Bash and Python tooling against a disposable or explicitly authorized SSH test host before considering operational use.",
  "url": "https://github.com/Guang84/SSH-Fleet-Doctor#installation",
  "steps": [
    "Use Linux, macOS, or WSL with Python 3, pip, an SSH client, and a test host you are authorized to administer.",
    "Clone the current SSH-Fleet-Doctor repository and inspect SETUP.sh, SSH_Infiltrator.sh, and python/SSH_Intruder.py before executing them.",
    "Run SETUP.sh to create the venv and install Paramiko, then activate the generated environment.",
    "Start the Python monitor with the test host IP and restricted username. Omit the password argument so the tool prompts instead of exposing it in shell history.",
    "Begin with read-only resource monitoring; review logs and remote permissions before enabling administrative operations.",
    "Prefer SSH keys, host-key verification, a restricted remote account, and tested backups for any non-disposable system."
  ],
  "command": "git clone https://github.com/Guang84/SSH-Fleet-Doctor.git\ncd SSH-Fleet-Doctor\nchmod +x SETUP.sh SSH_Infiltrator.sh\n./SETUP.sh\nsource venv/bin/activate\npython3 python/SSH_Intruder.py -i <authorized-host-ip> -u <restricted-user>",
  "projectId": "ssh-fleet-doctor",
  "audience": "Administrators or learners testing SSH monitoring against a disposable or explicitly authorized host.",
  "prerequisites": [
    "Linux, macOS, or WSL",
    "Python 3 and pip",
    "An SSH client",
    "A restricted account on a test host you are authorized to administer"
  ],
  "verification": [
    "The Python virtual environment activates and Paramiko imports successfully",
    "Read-only CPU, memory, disk, and network checks work on the test host",
    "Logs contain useful context without credentials or secrets"
  ],
  "troubleshooting": [
    "For connection failures, verify host reachability, SSH service status, username, and host-key expectations.",
    "For dependency failures, recreate the virtual environment and install from the project setup script.",
    "For production-like hosts, use SSH keys and a restricted account rather than command-line passwords."
  ]
}
